import { Position, Size } from "knowlege-gragh-canvas/network-diagram/index.type";
import { NetworkDiagramLayout } from "knowlege-gragh-canvas/network-diagram/layout/layout.type";
import { EdgeRenderingModule } from "knowlege-gragh-canvas/network-diagram/renderingLayer/edgeRenderingModule";
import { NodeRenderingModule } from "knowlege-gragh-canvas/network-diagram/renderingLayer/nodeRenderingModule";
export declare class RenderingLayer {
    private _size;
    private _offscreenCanvas;
    private _nodeRenderingModule;
    private _edgeRenderingModule;
    private _scale;
    constructor({ layout, scale, }: {
        layout: NetworkDiagramLayout;
        scale: number;
    });
    get nodeModule(): NodeRenderingModule;
    get edgeModule(): EdgeRenderingModule;
    setSize(size: {
        width: number;
        height: number;
    }): void;
    render(scale?: number): void;
    getElementId(position: Position): string | undefined;
    get offscreenCanvas(): OffscreenCanvas;
    get size(): Size;
}
export interface RenderingModule {
    getElementId(position: Position): string | undefined;
    offscreenCanvas: OffscreenCanvas;
    render(scale?: number): void;
}
