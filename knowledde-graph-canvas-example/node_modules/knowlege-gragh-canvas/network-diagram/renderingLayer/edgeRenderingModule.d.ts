import { DefaultEdge } from "knowlege-gragh-canvas/network-diagram/edge/defaultEdge";
import { NetworkDiagramEdgeMeta } from "knowlege-gragh-canvas/network-diagram/edge/edge.type";
import { Position, Size } from "knowlege-gragh-canvas/network-diagram/index.type";
import { RenderingModule } from "knowlege-gragh-canvas/network-diagram/renderingLayer/renderingLayer";
export declare class EdgeRenderingModule implements RenderingModule {
    private _offscreenCanvas;
    private _edges;
    private _scale;
    constructor({ size, edges, scale, }: {
        size: Size;
        edges: NetworkDiagramEdgeMeta[];
        scale: number;
    });
    addEdges(edges: NetworkDiagramEdgeMeta[]): void;
    get edges(): DefaultEdge[];
    setSize(size: Size): void;
    getElementId(position: Position): string | undefined;
    get offscreenCanvas(): OffscreenCanvas;
    render(scale?: number): void;
}
